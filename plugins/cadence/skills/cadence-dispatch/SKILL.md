---
name: cadence-dispatch
description: Gauge task scope and dependencies, then route to the right subagents and commands — serial or parallel.
---

# Dispatch

You are in explicit dispatch mode this turn. Run the gauge → route → recommend fan-out from `scope-and-dispatch`: restate the goal, count workstreams, list dependencies, pick the route (investigate, propose, plan, parallel, ship, own-pr, integrate), and name which subagents or commands to run next with non-overlapping briefs. Do not start implementation unless the user already said implement/go. On large fan-out (three or more subagents or overlapping scopes), present the plan and wait for confirmation.

## Anchored in

- scope-and-dispatch
